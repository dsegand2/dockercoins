apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  selector: 
    matchLabels: 
      app: worker
  template:
    metadata: 
      labels: 
        app: worker
    spec:
      containers: 
        -
          image: openshiftacademiaonline/dockercoins-ops:worker-1.3
          name: python
